package com.app.willywonkafactory;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class WillyWonkaFactoryApplicationTests {
	/*
	 * @Test void contextLoads() {}
	 * 
	 * @Autowired private MockMvc mockMvc;
	 * 
	 * @Autowired private ObjectMapper objectMapper;
	 * 
	 * @MockBean private WorkerServiceImpl workerService;
	 * 
	 * @Test public void test001_invalidInput_returnError() throws Exception {
	 * 
	 * WorkerDto dto = new WorkerDto(); dto.setName("test");
	 * 
	 * String writeValueAsString = objectMapper.writeValueAsString(dto);
	 * RequestBuilder requestBuilder = post("/worker").contentType(APPLICATION_JSON)
	 * .content(writeValueAsString) .accept(APPLICATION_JSON);
	 * 
	 * MvcResult result =
	 * mockMvc.perform(requestBuilder).andExpect(status().isOk()).andReturn();
	 * assertNotNull(result.getResponse());
	 * 
	 * }
	 */
}
